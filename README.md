1. install  prebuilt node from https://nodejs.org/en/download LTS version .msi installer for windows
2. open the terminal/cmd in file directory and run "npm install" to install node modules
3. run start.bat or open terminal in directory and run "node server.js"
